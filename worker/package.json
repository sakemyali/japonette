{
  "name": "japonette-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that brokers 42 OAuth for the japonette CLI.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240603.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.65.0"
  }
}
